---

# 🔹 Phase 0 — Specification Hardening (חובה בכל פרויקט לא טריוויאלי)

מטרה: לוודא שה-PRD חד, סגור, וללא חורים לוגיים לפני שמתחילים לתכנן.

Persona: **Sisyphus – qwen3-coder-next-64K**
Context Window:

* קטן: 32K
* בינוני: 48K
* גדול: 48K

Agent: `@plan`

Task:

```
Read the PRD carefully.

Identify:
- Ambiguities
- Missing assumptions
- Hidden dependencies
- Logical gaps
- Risk areas

List all unclear decisions that must be resolved before planning.
Do NOT propose implementations.
Do NOT write code.
```

---

# 🔹 Phase 1 — Deterministic Planning

מטרה: להפוך את ה-PRD לצ'ק-ליסט יישומי קשיח ואטומי.

Persona: **Sisyphus – qwen3-coder-next-64K**
Context Window:

* קטן: 32K
* בינוני: 48K
* גדול: 48K

Agent: `@plan`

Task:

```
Based on the validated PRD,
produce a strict, ordered implementation checklist.

Each step must:
- Be concrete
- Be independently implementable
- Have a clear completion condition

Do NOT write code.
Do NOT merge multiple logical steps into one.
```

---

# 🔹 Phase 2 — Architecture & Responsibility Design

מטרה: להחליט איך המערכת בנויה לפני כתיבת קוד.

Persona: **Sisyphus – qwen3-coder-next-64K**
Context Window:

* קטן: 32K
* בינוני: 48K
* גדול: 48K

Agent: `@plan`

Task:

```
Using the approved checklist,
define the full project structure.

For each module:
- Define responsibility
- Define inputs and outputs
- Define what it must NOT do

Clearly separate:
- Core logic
- Policy
- Validation
- Infrastructure

Do NOT write code.
```

---

# 🔹 Phase 3 — Interface & Schema Lock

מטרה: לנעול חוזים, מבני נתונים ופורמטי החזרה לפני מימוש.

Persona: **Sisyphus – qwen3-coder-next-64K**
Context Window:

* קטן: 24K
* בינוני: 32K
* גדול: 32K

Agent: `@plan`

Task:

```
Define all interface contracts.

For each module:
- Input schema
- Output schema
- Error format
- Validation rules

Use structured JSON schemas where relevant.

Do NOT write implementation code.
```

---

# 🔹 Phase 4 — Module Implementation (מודול אחד בכל פעם)

מטרה: לממש רק את המודול המבוקש, ללא חריגה מהתכנון.

Persona:

* ברירת מחדל: **Hephaestus – qwen3-coder-30b-32k**
* מורכב / state רחב: **Hephaestus – qwen3-coder-next-64K**

Context Window:

* קטן: 24K
* בינוני: 32K
* גדול: 32K–48K

Agent: `@prometheus`

Task:

```
Implement ONLY the specified module from the checklist.

Follow the predefined architecture and contracts exactly.
Do NOT modify other modules.
Do NOT introduce new features.
Stop after completing this module.
```

---

# 🔹 Phase 5 — Unit Tests (מודולרי)

מטרה: לבדוק התנהגות לפני חיבור שכבות.

Persona: **Hephaestus – qwen3-coder-30b-32k**
Context Window:

* קטן: 16K–24K
* בינוני: 24K–32K
* גדול: 32K

Agent: `@prometheus`

Task:

```
Write unit tests for the implemented module.

Test:
- Normal cases
- Edge cases
- Failure scenarios

Do NOT refactor production code unless strictly required.
Do NOT expand scope.
```

---

# 🔹 Phase 6 — Integration Review

מטרה: לבדוק זרימת נתונים, הפרדת שכבות והיעדר coupling סמוי.

Persona: **Atlas – mistral-small3.2-64k**
Context Window:

* קטן: 32K
* בינוני: 48K
* גדול: 64K

Agent: `@oracle`

Task:

```
Review the full system integration.

Analyze:
- Data flow consistency
- Boundary violations
- Hidden coupling
- Policy enforcement correctness

Do NOT rewrite code.
Do NOT propose new features.
Only identify structural issues and risks.
```

---

# 🔹 Phase 7 — Adversarial Audit (Anti-Hallucination / Robustness)

מטרה: לנסות לשבור את המערכת ולזהות חורי לוגיקה.

Persona: **Atlas – mistral-small3.2-64k**
Context Window:

* 32K–48K
* 64K רק אם מדובר במערכת רחבה מאוד

Agent: `@oracle`

Task:

```
Act as an adversarial reviewer.

Attempt to break the system by:
- Supplying out-of-scope inputs
- Ambiguous cases
- Partial context
- Boundary violations

Identify any scenario where the system may behave incorrectly.
Do NOT rewrite the system.
```

---

# 🔹 Phase 8 — Deep Explanation Mode (שלב למידה)

מטרה: להבין לעומק את מה שנבנה כדי שתוכל להסביר ולהשתמש בו.

Persona: **Atlas – devstral-small-2-latest-64k**
Context Window:

* 24K–32K

Agent: `@librarian`

Task:

```
Explain the system in depth.

For each module:
- Why it exists
- What problem it solves
- What would break if removed
- How it interacts with other modules

Use clear structured explanations.
Do NOT modify the system.
```

---

# 🔹 Phase 9 — Documentation Freeze

מטרה: תיעוד רשמי מוכן לשימוש עתידי.

Persona: **Atlas – devstral-small-2-latest-64k**
Context Window:

* 24K–32K

Agent: `@librarian`

Task:

```
Generate formal project documentation.

Include:
- Architecture overview
- Module descriptions
- Data flow explanation
- Usage instructions
- Configuration explanation

Produce DOCUMENTATION.md only.
Do NOT modify code.
```
